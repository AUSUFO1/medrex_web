'use client';

export default function HeroHeadline() {
  return (
    <h1 className="mt-5 text-[clamp(2.65rem,10vw,7.25rem)] font-medium leading-[0.94] tracking-[-0.08em] sm:mt-6 sm:text-[clamp(3.25rem,11vw,7.25rem)]">
      Health at the
      <br />
      speed of clarity
    </h1>
  );
}
